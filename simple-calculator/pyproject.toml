[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "simplecalc-pypi-test"          # УНИКАЛЬНОЕ ИМЯ!
version = "0.1.0"
description = "A simple calculator package"
authors = [{name = "Sn1pe1t", email = "enderman303040@gmail.com"}]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.urls]
Homepage = "https://github.com/yourusername/simple-calculator"   # замените